# Session Log

> Part of social-publisher/.project-knowledge/ | Last updated: 2026-07-10 (Instagram publish race condition fixed, stale-webhook-registration bug fixed)
> Append-only — never edit past entries.

| Date | Summary |
|------|---------|
| 2026-07-02 | Diagnosed and fixed the full Telegram approval chain: confirmed trust-proxy was already fixed, resolved a rate-limit/crash loop by resetting the webhook and recreating the container, fixed a stale-session permission error, fixed a truncated Supabase API key + missing `Bearer` prefix, fixed unset field expressions in `Extract Command`, and fixed a silent-no-reply bug via "Always Output Data". Set up this repo (`social-publisher`) with exported workflows and `.project-knowledge/`, since n8n's native git push isn't available on this license. |
| 2026-07-03 | Got WF1 (LinkedIn Post - Personal) publishing and drafting end-to-end: fixed a missing Telegram credential on 4 nodes, fixed unset `Extract Content` field expressions (same bug class as WF4), and added a missing `Extract Post Text` node that 6 other nodes referenced but never existed. Diagnosed each step directly from the sqlite execution data rather than trusting the UI, since several rounds of manual expression edits silently saved as literal strings instead of real expressions. Confirmed via live Telegram test: `y: <topic>` now generates a real LinkedIn post via Groq and returns a preview. Along the way, discovered WF1 and WF4 silently contend for the same Telegram webhook (logged as a bug in `roadmap.md`) — `/approve` won't reach WF4 until that's resolved. Exported the updated workflow to `workflows/linkedin-post-personal.json` and pushed. |
| 2026-07-03 (cont.) | Closed the loop: merged WF4's approve-handling nodes directly into WF1 (copy/paste across tabs) to fix the webhook-contention bug, since two workflows can't each own a Telegram Trigger on the same bot. Then chased a second, gnarlier bug — `/approve` kept 404ing on resume with "does not contain a waiting webhook with a matching path/method" even against fresh, never-edited executions — eventually root-caused via direct curl testing to n8n's `Wait` node defaulting to `GET` for its resume webhook, while `Resume Workflow` had always sent `POST`. Fixed by switching to GET + query params, then fixed a follow-on bug where `Extract Command`'s `command` field kept its leading `/` (breaking both `Route Command`'s matching and a Supabase check constraint), plus one more recurrence of the double-`=` expression mistake. **First real end-to-end success**: `y: ...` → Groq draft → Supabase row → Telegram preview → `/approve` → actually posted to personal LinkedIn → Telegram confirmation. Re-exported both workflows (WF1 now self-contained with 24 nodes; WF4 kept inactive as `command-listener-deprecated.json`), redacted secrets again, and pushed. |
| 2026-07-03 (cont. 2) | Polish pass on the now-working loop: fixed `Update Status` writing an invalid `status` value (mapped `approve`/`discard` to `approved`/`rejected` instead of the raw command word). Rewrote the Groq system prompt twice based on live test output — first to stop it defaulting to Synax/work themes regardless of topic, then again to stop it opening every post with the identical "Just spent [time]..." hook. Also fixed a latent bug in `Rewrite with Instructions` (still read `$json.body.instructions` after the query-param switch). Confirmed each fix by reading the DB directly rather than trusting "done" — two of the three edits this round silently failed to save on the first attempt (tested against old unsaved text) and needed a retry. Discussed and declined building LinkedIn hashtag-popularity lookup (no public API, scraping would violate ToS). Verified the exposed Supabase key is confined to the repo's first commit only; recommended rotation, awaiting a decision on git-history rewrite. Re-exported, redacted, and pushed. |
| 2026-07-04 | Built and merged WF2 (Instagram) into WF1, same reasoning as the WF4 merge — two Telegram Triggers on one bot don't coexist. Merge was done by editing a user-supplied workflow-export file directly into a paste-ready fragment (renamed 8 colliding nodes with an `(IG)` suffix, fixed its broken `/regenerate` wiring, baked in the real Telegram credential) rather than manual node-by-node rebuilding. Hit and fixed a new bug class: the source file's Set nodes used a `{name, type, value}` schema that silently doesn't work on this n8n version's typeVersion 3 Set node (needs `{name, stringValue}`) — three nodes were passing through input completely unchanged with no error, until fields were re-entered through the actual node UI. Also fixed `Send Preview (IG)` referencing a nonexistent `photoUrl` parameter (confirmed correct one, `file`, straight from the installed node's source code) and a Meta/Instagram access token saga: first an expired short-lived token, then a stale-execution-snapshot false alarm, then a "cannot parse" error from 2 stray leading spaces — resolved once the user supplied a proper long-lived Page Access Token and pasted it cleanly. **First real Instagram post published live.** Remaining open problem: image/caption/hashtag quality is still bad per the user despite two prompt-tuning rounds — logged as an open bug rather than claimed fixed. Also scoped two future features (post-user's-own-photo, AI image-to-image) after discussing Telegram's file-download support. Re-exported all three workflows, redacted secrets (now including the Meta token), updated docs, and pushed. |
| 2026-07-04 (cont.) | Made the repo public-ready: web-searched confirmed the repo history still had the real Supabase key exposed in the first commit, so rewrote all git history with `git filter-branch` to scrub it, force-pushed, verified clean on GitHub, and cleaned up local backup refs. Also genericized every account-specific ID (Supabase ref, LinkedIn URN, Instagram Business Account ID, Cloudinary cloud name/preset) out of the README and workflow exports at the user's request, and fixed a misleading mermaid diagram that didn't clearly separate the LinkedIn/Instagram paths. Removed the two now-pointless deprecated workflow export files from the repo. Then actually solved the Instagram content-quality problem left open earlier: web-researched FLUX prompting (natural language over keyword tags, subject-first ordering, concrete camera/lighting vocabulary) and Llama 3.3 prompting (few-shot examples beat abstract rules) and rewrote `Build Image Prompt` and `Generate Caption` accordingly, plus fixed a hashtag-formatting bug (stray space after `#`, hyphens breaking tag parsing) and replaced a rigid length cap with one that scales to the topic. **Confirmed live: both a punchy short post and a longer reflective one now produce genuinely well-composed, on-topic images and correctly-formatted captions.** Renamed the exported workflow file from `linkedin-post-personal.json` to `main-workflow.json` to reflect that it now covers every platform. |
| 2026-07-04 (cont. 2) | User asked whether hashtag *count* (not just formatting) needed a second look, and whether the LinkedIn prompt had gotten the same hashtag-format fix as Instagram — it hadn't. Web research turned up something concrete: Instagram now enforces a hard 5-hashtag cap (Dec 2025) and both platforms' own engagement data favor 3-5 specific hashtags over many generic ones, which directly explained why `Generate Caption` (still asking for 15-20) was underperforming. Fixed the count there, and added the same hashtag-formatting rule to both LinkedIn prompts preventively (they were already correctly set to 3-5, just missing the explicit no-space/no-hyphen rule). User applied and confirmed all three edits before this was logged. Re-exported, redacted, updated docs, pushed. |
| 2026-07-05 | User reported `/approve` silently stopped working. Root-caused via direct execution inspection (had to re-copy the sqlite DB including its `-wal`/`-shm` files each time — an earlier snapshot without them looked stale and briefly caused a wrong read): `Send Updated Preview` (fired after `/edit`) had no outgoing connection at all, so the execution just finished instead of looping back to `Wait for Approval`; the next `/approve` then tried to resume that dead execution and 409'd silently. Fixed by wiring `Send Updated Preview → Wait for Approval`. User then hit a second, unrelated problem on the same `/edit` path: with no instructions given, the rewrite kept reusing the original post and producing near-duplicate synonym-swapped text, then progressively fabricated specifics never mentioned in the one-line input, wrapped in generic AI-startup jargon and a repeated rhetorical-question ending. Web-researched hallucination-mitigation and AI-slop/cliché-avoidance techniques and rewrote both `Generate LinkedIn Post` and `Rewrite with Instructions`'s system prompts with an explicit grounding rule and a named banned-phrase list, plus a ternary in the rewrite prompt so a bare `/edit` demands a genuinely different take. Two paste mistakes happened applying this (wrong node's body pasted into the other; a leftover leading `=` doubled into `==`, silently corrupting the JSON sent to Groq) — both diagnosed by reading the live node's stored body param directly rather than guessing, matching the project's established "doubled `=`" bug class. Confirmed fully working end-to-end (exec 99: draft → `/edit` → `/approve` → posted to LinkedIn). Re-exported, redacted, updated docs, pushed. |
| 2026-07-05 (cont.) | User reported `ig:` drafts stopped replying — turned out `Generate Caption` had the same doubled-`=` corruption left over from an earlier editing session, unnoticed since Instagram hadn't been retested. Fixed, then tackled a real feature request: independently regenerating just the image or just the caption instead of always both. Built via two new If nodes (`Route Regenerate Target`, `Skip Caption?`) reusing the existing `instructions` field from `/edit`'s plumbing — hit the doubled-`=`/stray-space bug twice more while wiring them, plus a genuinely new bug (referencing a Wait node's data before it had ever executed, on the very first draft pass, threw instead of returning undefined — fixed with a try/catch IIFE). User pushed back hard on relying on typed trailing text, since Telegram's tappable `/word` suggestions send immediately without letting you type more after tapping — investigated and found this project has **zero** BotFather-registered commands at all; the tappable words are just Telegram auto-linking any `/word` pattern inside message text. Redesigned around that: replaced the two-hop `instructions`-matching approach with dedicated one-word commands (`/regenerateImage`, `/regenerateCaption`) as direct `Route Command (IG)` switch branches, which then hit one more bug (a case mismatch, `regenerateimage` vs `regenerateImage`, on a case-sensitive comparison) before working end-to-end, confirmed via per-node run counts in the DB. Also fixed a real content-quality issue: Instagram images occasionally showed disturbing/uncanny realistic human faces (a known FLUX failure mode) — web-researched and confirmed the fix isn't a realism dial but avoiding the semi-realistic middle ground entirely, added a rule to `Build Image Prompt` favoring faceless/distant/stylized depictions of people. Hit the recurring unescaped-inner-quote mistake while writing that fix (single-quoted phrases like `'35mm'` inside an already single-quoted JS string need `\'` escaping) — caught by n8n's red "invalid syntax" indicator, fixed by properly escaping all inner quotes. Re-exported, redacted, updated docs, pushed. |
| 2026-07-08 | Large session covering several rounds of feature work, all built via n8n's Public API (self-generated key) rather than manual UI clicks, given the scale. Started with LinkedIn image support (`Register LinkedIn Upload`/`Upload Image to LinkedIn`/`Post to LinkedIn (Image)`, mirroring Instagram's Cloudinary pipeline but using LinkedIn's own asset-upload API) plus a `yraw:`/`igraw:` raw-text mode and Telegram inline-keyboard (`callback_query`) support for a 2-button image-decision prompt — hit and worked through an n8n node-group topology validation error along the way (each canvas group must have exactly one external entry/exit; wrote a local Python re-implementation of the validator to check changes before pushing instead of guessing via failed round-trips). First live test revealed a real design flaw: the generated LinkedIn image published without ever being shown first, and it was disturbing enough that the user had to delete the post — fixed by always showing the real generated photo in Telegram before any publish is reachable, matching Instagram's existing pattern. A follow-up prompt-hardening edit (banning the specific facial-expression + photorealistic-language combination that caused that image) introduced a regression that broke image generation entirely for a few minutes (unescaped quotes spliced into an already-escaped body string) — caught immediately via execution inspection and fixed. User then proposed replacing the whole typed-command/mixed-button UX with one consistent 5-button menu (regenerate-both / image-only / text-caption-only / post / discard) on both platforms, retiring `/approve`/`/edit`/`/regenerate*` entirely — built for LinkedIn first (removing the upfront image-decision gate in favor of image-as-a-regenerate-option, since LinkedIn posts are optionally-imaged) then Instagram (a much smaller change, reusing the already-existing `Skip Caption?`/shared-`Build Image Prompt`-reentry mechanism, just repointing command values). Immediately after, user asked to also drop `yraw:`/`igraw:` as separate prefixes and make verbatim-by-default the behavior of plain `y:`/`ig:` instead — deleted `Generate LinkedIn Post`/old `Extract Post Text`/`Extract Content`/`Has yraw: prefix?` as now-dead nodes and simplified several try/catch reference chains down to their necessary cases. Testing this surfaced two more real bugs, both fixed: sending a photo with an `ig:`-prefixed caption fell through to the "wrong format" reminder because prefix checks only read `message.text`, never `message.caption` (a bug already logged in `roadmap.md` since 2026-07-04, now actually fixed as part of building genuine "post your own photo" support — download via Telegram's file API, upload to Cloudinary, same downstream pipeline); and Instagram's "regenerate caption" button crashed silently on any user-photo post since `Generate Caption` referenced a node (`Extract Image Prompt`) that never runs on that path. Finished with a UX polish the user asked for directly: button taps now show an immediate Telegram toast confirming which action registered, instead of only the button's own easy-to-miss loading-spinner animation. Re-exported, redacted, updated all `.project-knowledge/` files, committed. |
| 2026-07-10 | User reported an Instagram post attempt "didn't work" and asked why Telegram showed a menu of unrelated commands ("prisma", "supabase skills", etc.) under a chat named "The Bear Code". Root-caused the real bug via the failed execution's error log: Instagram's Graph API rejected `/media_publish` with "Media ID is not available" because it was called immediately after `Create Media Container`, before the image finished processing — a genuine race condition, not a config mistake. Fixed by inserting a poll-until-`FINISHED` loop (`Wait Before Media Check` → `Check Media Status` → `Track Poll Attempt` → `Route Media Status` → publish or `Notify Media Failed (IG)`), verified locally against a from-source-accurate Python re-implementation of n8n's node-group validator before pushing (passed first try). Investigated the "weird menu" as a possible second system stealing the bot's webhook; ruled that out concretely (grepped the whole workflow for `setMyCommands`, confirmed only one active workflow in the n8n instance, checked for competing local processes/containers) and instead found a second, real, unrelated bug via ngrok's request inspector: some genuine Telegram messages got a `200` response with a bare `firstEntryJson` body and created no execution at all — traced to a stale webhook registration after several structural API `PUT`s to the already-active workflow, fixed by deactivating/reactivating it. The "Bear Code" menu itself turned out to be the user's own personal Telegram username (`TheBearCode`, confirmed from the raw webhook payload, `is_bot: false`) showing up in a chat header — a red herring the user resolved themselves by clearing it via BotFather. Re-exported, redacted, updated docs, committed. |
| 2026-07-12 | User got an OpenRouter API key set up for captions and asked to move Instagram caption generation off Groq/Llama onto it. Switched `Generate Caption`'s HTTP node from `https://api.groq.com/openai/v1/chat/completions` (`llama-3.3-70b-versatile`) to `https://openrouter.ai/api/v1/chat/completions` (`openai/gpt-4o-mini`), swapped the hardcoded `Authorization` header to the new OpenRouter key, kept `max_tokens: 700` and the existing system prompt as-is (already plain OpenAI-format chat messages, no Llama-specific quirks to strip). Confirmed the response shape (`choices[0].message.content`) is identical so `Set Post Data` needed no change. Applied via the Public API (same deactivate/reactivate cycle as 2026-07-10, to avoid repeating that stale-webhook bug after a structural PUT). Only `Generate Caption` moved — LinkedIn text and both image-prompt nodes stay on Groq/Llama. Re-exported, redacted, updated docs, committed. |
